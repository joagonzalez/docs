version: "3"
services:
  mkdocs:
    image: "${VERSION_DOCS}" #latest
    deploy: 
      placement: 
        constraints: 
          - node.platform.os == linux
          - node.role == worker
    ports:
      - "8002:8000"
    # volumes:
    #   - /shared/newcos-docs:/newcos/docs
    networks:
      - dmz  

networks:
  dmz:
    external: true